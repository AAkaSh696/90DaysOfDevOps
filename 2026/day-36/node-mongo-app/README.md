This is a simple project using node.js and mongodb . It is used for practicing docker and for day-36 task of #90daysofdevops challenge .

To run these app:

- Make sure docker is installed.
- Have docker-compose.yml file
- Run command : docker compose up --build
