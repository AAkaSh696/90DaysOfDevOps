server:
  name: myweb
  ip: 192.168.42.1
  port: 80

database:
  host: localhost
  name: my-database
  credentials:
    user: alex
    password: Test@123   

startup_script_pipe: |
  echo "Starting server..."
  echo "Server Procesing"
  echo "Server started"

startup_script_fold: >
  docker run -it --network my-net
  -p 80:80
  -v myvol:/lib/db
